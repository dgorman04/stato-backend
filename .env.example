# Copy to .env for local overrides (optional).
# On Railway, set these in the dashboard Variables.

# SECRET_KEY=your-secret-key-here
# DEBUG=false
# ALLOWED_HOSTS=yourapp.up.railway.app
# DATABASE_URL=postgres://...  (set automatically when you add Postgres on Railway)
# REDIS_URL=redis://...        (optional; only if you add Redis)
# CSRF_TRUSTED_ORIGINS=https://yourapp.up.railway.app
